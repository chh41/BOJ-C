#include <stdio.h> 
#include <math.h>
int main (void) 
{ 
    int r, t = 0;

    scanf("%d", &r);
    
    for (int i = 1 ; i <= r; i++)
    {
        t += i;
    }
    printf("%d\n", t);

    return 0;
}
