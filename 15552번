#include<stdio.h>

int main(void)
{
    
    int a;
    int b,c;

    scanf("%d", &a);

    for (int i=1;i<=a;i++)
    {
        scanf("%d %d", &b, &c);    # c언어의 scanf, printf는 충분히 빠르다
        printf("%d\n", b+c);
    }
}
