#include <stdio.h> 
int main (void) 
{ 
    int a;
    int b;

    scanf("%d %d", &a, &b);      # 파이썬은 map함수를 사용해서 두 개 이상의 입력값을 받아야되지만
                                 # c언어는 바로 %d 두번으로 입력받을 수 있음
    printf("%d \n", a+b);

    return 0;
}
