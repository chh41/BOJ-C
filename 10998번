#include <stdio.h>             # 표준 입출력 헤더파일 선언
int main (void) 
{ 
    int a;                     # a,b값을 int형으로 선언은 했지만 초기화 안했기때문에 쓰레기값을 가지고 있음
    int b;

    scanf("%d %d", &a, &b);    # a,b값을 입력받아 초기화

    printf("%d \n", a*b);      # a*b값 출력

    return 0;
}
