#include <stdio.h> 
int main (void) 
{ 
    int a;
    int b;

    scanf("%d %d", &a, &b);

    printf("%.9lf\n", (double)a/(double)b);    # (double)로 형변환 해주기. 문제에서 10^(-9) 오차까지 인정이라고 해서 .9lf로 소숫점 9번째자리까지 출력

    return 0;
}
